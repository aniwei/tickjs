import {
  TokenTypes
} from '../JSToken'

export function parseStatement (token) {
  switch (token.value) {
    case '{':
      // block
      break;

    case TokenTypes.TOKEN_RETURN: {

    }

    case TokenTypes.TOKEN_THROW: {

    }

    case TokenTypes.TOKEN_VAR: {
      
    }
  }
}