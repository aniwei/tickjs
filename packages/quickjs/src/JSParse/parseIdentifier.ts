const ASCII = 128

export function isIdentifierNext () {}

export function parseIdentifier (char) {
  const buffer: string[] = [];

  done: {

  }

  while (true) {
    if (char < ASCII) {
      buffer.push(char);
    } else {

    }

    if (char === '\\' &&  p === 'u') {
      // unicode
    }

    if (!isIdentifierNext(c)) {
      break;
    }
  }

  const atom = JSNewAtomLen()
}