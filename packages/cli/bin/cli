#!/usr/bin/env node
const commander = require('commander');

const commands = require('../dist/commands');

commander
  .command('start')
  .action((...argv) => {
    commands.start(...argv);
  })

commander
  .command('init')
  .action((...argv) => {
    commands.init(...argv)
  })

commander.parse(process.argv);