import {
  invokeCallbackHandler
} from '../../MiniProgram'

export const WeixinJSCore = {
  invokeHandler (miniProgram, name, options, callbackId) {
    
  },
  
  publishHandler (miniProgram, name, options, callbackId) {
  
  }
}
