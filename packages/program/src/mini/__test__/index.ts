import fs from 'fs-extra';
import { resolve } from 'path';


/*
  const program = new Program();
  
  program.unpack(wxakpg);
    await program.source({
      service: 'app-service.js'
      config: 'app-config.json',
      page: 'app-wxss.js',
    })
  
  program.launch({
    ...config
  });

  program.invoke('launch', => {
    cosnt page = await page.create();

    service.subscribe();

  })

  program.action('onAppRoute', () => {

  });


  service.subscribe('onAppRoute', { path: '' }, webviewId, []);

*/

